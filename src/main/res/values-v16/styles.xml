<resources>

    <!--
        Base application theme for API 16+. This theme completely replaces
        AppBaseTheme from res/values/styles.xml on API 11+ devices.
    -->
    <style name="AppBaseTheme" parent="android:Theme.Holo.Light">
        <item name="android:buttonStyle">@style/TemakiButtonStyle</item>
        <item name="android:textViewStyle">@style/TemakiTextViewStyle</item>
        <item name="android:editTextStyle">@style/TemakiEditTextStyle</item>
    </style>

    <style name="FocusTheme" parent="android:Theme.Holo">
        <item name="android:actionBarStyle">@style/TemakiActionBarFocusStyle</item>
        <item name="android:buttonStyle">@style/TemakiButtonStyle</item>
        <item name="android:textViewStyle">@style/TemakiTextViewStyle</item>
        <item name="android:editTextStyle">@style/TemakiEditTextStyle</item>
    </style>

    <!-- TextView Style -->
    <style name="TemakiTextViewStyle" parent="android:Widget.TextView">
        <item name="android:fontFamily">sans-serif-light</item>
    </style>

    <!-- Button Style -->
    <style name="TemakiButtonStyle" parent="android:Widget.Button">
        <item name="android:fontFamily">sans-serif-light</item>
    </style>

    <!-- EditText Style -->
    <style name="TemakiEditTextStyle" parent="android:Widget.EditText">
        <item name="android:fontFamily">sans-serif-light</item>
    </style>
</resources>
